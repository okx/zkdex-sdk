/// the start number of layer2 block
pub const START_BLOCK_NUMBER: i64 = 1;

/// tx timestamp value when we fist time run the system.
pub const GENESIS_TX_TIMESTAMP: u64 = 0;

/// the start id of layer2 transaction
pub const START_TX_ID: i64 = 0;
